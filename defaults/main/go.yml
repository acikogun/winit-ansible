---
# Default go settings

go:
  api_url: https://golang.org/VERSION?m=text
  install: /usr
  base_url: https://dl.google.com/go
  file_suffix: .linux-amd64.tar.gz
  symlinks:
    - {src: /usr/go/bin/go, dest: /usr/bin/go}
    - {src: /usr/go/bin/gofmt, dest: /usr/bin/gofmt}
