---
# Default go settings

go:
  install: /usr
  src: https://dl.google.com/go/{{ go_version.content }}.linux-amd64.tar.gz
  dest: /tmp/{{ go_version.content }}.linux-amd64.tar.gz
  symlinks:
    - {src: /usr/go/bin/go, dest: /usr/bin/go}
    - {src: /usr/go/bin/gofmt, dest: /usr/bin/gofmt}
